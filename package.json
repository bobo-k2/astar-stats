{
  "name": "astar-stats",
  "version": "0.0.4",
  "description": "A astar network blockchain aggregation data.",
  "main": "dist/index.js",
  "scripts": {
    "build": "subql build",
    "codegen": "subql codegen",
    "start:docker": "docker-compose pull && docker-compose up --remove-orphans",
    "dev": "subql codegen && subql build && docker-compose pull && docker-compose up --remove-orphans",
    "prepack": "rm -rf dist && npm run build",
    "test": "jest"
  },
  "homepage": "https://github.com/subquery/astar-subql-starter",
  "repository": "github:subquery/astar-subql-starter",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "AstarStats",
  "license": "MIT",
  "devDependencies": {
    "@polkadot/api": "^9",
    "@subql/cli": "latest",
    "@subql/types": "latest",
    "typescript": "^4.1.3"
  },
  "exports": {
    "chaintypes": "src/chaintypes.ts"
  },
  "dependencies": {
    "@subql/contract-processors": "^0.6.0",
    "abab": "^2.0.6"
  }
}
